#!/bin/sh
# lint gate — the one check that slipped past local discipline before
cd "$(git rev-parse --show-toplevel)" && pnpm exec biome check . || {
  echo "pre-push: lint failed (bypass only with --no-verify; CI will still fail)"; exit 1; }
